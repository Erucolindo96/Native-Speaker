#ifndef TEMPDIRMANAGER_HPP
#define TEMPDIRMANAGER_HPP
#include"record-base/ModelDirManager.hpp"

class TempDirManager : public ModelDirManager
{
public:
//  const std::string TEMP_DIR_NAME = "@temp";

//  TempDirManager(ConfigManager conf = ConfigManager());
//  TempDirManager(const TempDirManager &other) = default;
//  TempDirManager& operator=(const TempDirManager &other) = default;
//  TempDirManager(TempDirManager &&other) = default;
//  TempDirManager& operator=(TempDirManager &&other) = default;

//  virtual void createTempDir();
//  virtual void cleanTempDir();
//  virtual void removeTempDir();
//  void createModelDir(const std::string &model_name) override;
//  void removeModelDir(const std::string &model_name) override;
//  void cleanModelDir(const std::string &model_name) override;


};

#endif // TEMPDIRMANAGER_HPP
